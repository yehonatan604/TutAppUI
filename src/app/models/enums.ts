export enum UserStatusTypes {
    LoggedIn,
    LoggedOff
}

export enum UserTypes {
    Guest,
    Reader,
    Creator,
    Admin
}

export enum CategoryTypes {
    Programming,
    Internet,
    IOT,
    Design
}

export enum ArticlesListTypes {
    New,
    Starred,
    Visited
}

export enum SenderTypes {
    Home,
    Library
}

export enum FormTypes {
    Login,
    Register
}